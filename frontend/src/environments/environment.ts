/**
 * Base environment configuration
 * Used for production builds
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.nightreader.com', // Replace with your production API URL
  apiVersion: 'v1',
};
